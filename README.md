# Face Detection using OpenCV (Webcam)

## 📌 Overview
This project is a real-time face detection system built using Python and OpenCV.
It detects faces from a webcam feed and draws bounding boxes around detected faces.

## 🚀 Features
- Real-time face detection using webcam
- Uses Haar Cascade classifier
- Works using OpenCV built-in cascade path
- Exit using **Q** key or **ESC**

## 🛠️ Tech Stack
- Python
- OpenCV

## ⚙️ Installation
```bash
pip install -r requirements.txt
